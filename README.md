# ReadMe

# ActiveSec

Проект ActiveSec состоит из трёх подпроектов, каждый из которых выполняет отдельную функцию в системе безопасности и мониторинга.

## Подпроекты

### 1. ActiveSec driver 💻

**Описание:** Драйвер для мониторинга и блокировки действий с использованием minifilter.
**Функция:** Отслеживание и предотвращение нежелательных действий на уровне системы, включая асинхронное логирование операций чтения и записи через связку модулей `callback` и `worker`.

### Модули ActiveSec driver

### 1. common

Содержит основные константы проекта:

- `AS_TAG = "ceSa"` - тэг для выделения памяти.
- `MAX_LOG_BUFFER_LEN = 10240` - максимальная длина передаваемого буфера записи в UMAnalizer для анализа.
- `PORT_NAME = "\\ActiveSec"` - имя порта для обмена данными между драйвером и UMAnalizer.

### 2. callback & worker

Модуль `callback` реализует обратные вызовы для событий `PreCreate`, `PreRead` и `PreWrite`, при этом логируются все операции, а к операциям чтения и записи добавляется буфер, который они пытаются прочитать или записать. Модуль `worker` обеспечивает асинхронное выполнение этих логирующих задач, позволяя драйверу работать без блокировок.

### 3. comm (communicate)

Отвечает за отправку и приём логов:

- Отправка логов в виде структуры **KM_MESSAGE**:
    - `Type (ulong)` - тип действия (0 - create, 1 - read, 2 - write)
    - `ProcessPath (wchar[256])` - путь к исполняемому файлу
    - `FilePath (wchar[260])` - путь к файлу
    - `Offset (ulong)` - смещение для чтения/записи
    - `BufferLength (ulong)` - реальная длина буфера (<= MAX_LOG_BUFFER_LEN)
    - `Buffer (char[MAX_LOG_BUFFER_LEN])` - данные
- Асинхронный приём сообщений от UMAnalizer в виде структуры **UM_MESSAGE**:
    - `ProcessPath (wchar[256])` - путь к исполняемому файлу
    - `Type (ulong)` - 0 (добавить PID в blockedpid), 1 (удалить PID из blockedpid)

### 4. blockedpid

Реализует динамический список заблокированных процессов с функциями:

- `Add` - добавить процесс
- `Remove` - удалить процесс
- `Find` - проверить наличие процесса в списке

### 2. UMAnalizer 📊

**Описание:** Фоновая программа для анализа данных, полученных от ActiveSec driver.
**Функция:** Решение о блокировке некоторых процессов на основе анализа данных.

### Модули UMAnalyzer

### 1. Communication

Отвечает за отправку и приём логов (структура повторяет структуру в модуле ActiveSec driver)

### 2. MessageProcessor

В модуле реализована асинхронная очередь для обработки логов

### 3. BlackList

Модуль отвечает за хранение всех видов подозрительных данных в виде `Threats` и `Processes`. Threats и Processes хранятся в структуре типа HashSet.

- `Threat` - структура для хранения заблокированных процессов:
    - `ProcessPath (WCHAR[256])` - путь к исполняемому файлу
    - Тип угрозы *(не реализовано)*
- `Proccess` - структура для хранения информации о процессах:
    - `_pid (ulong)` - идентификатор процесса
    - `_distrust (double)` - уровень недоверия к процессу
    - `Actions (List&lt;Action&gt;)` - список действий, выполненных процессом
    - Методы:
        - `GetPid()` - получить идентификатор процесса
        - `AddAction(Action action)` - добавить действие и увеличить недоверие
        - `GetDistrust()` - получить уровень недоверия
        - `CompareTo(Proccess other)` - сравнить процессы по идентификатору
        - `Equals(Proccess other)` - проверить равенство процессов
- `Action` - структура для хранения действий процесса:
    - `Type (ushort)` - тип действия
    - `Distrust (float)` - уровень недоверия к действию
    - `Path (string)` - путь к файлу
    - Методы:
        - `CompareTo(Action other)` - сравнить действия по уровню недоверия
        - `Equals(Action other)` - проверить равенство действий

Методы модуля:

- `LoadThreads` - загрузка заблокированных угроз
- `LoadProcesses` - загрузка данных о отслеживаемых процессах
- `UpdateThreatsList` - добавление новой угрозы в файл
- `UpdateProcessList` - добавление данных о процессе в файл
- `GetProcess` - получение процесса из массива отслеживаемых
- `AddProcess` - добавление нового процесса в отслеживаемые (сопровождается  `UpdateProcessList`)
- `AddThreat` - добавление новой угрозы (сопровождается `UpdateThreatsList`)

### 4. CryptoAI

### 3. ActiveSec GUI🖥️

**Описание:** Визуальное отображение информации, собранной предыдущими подпроектами.
**Функция:** Обеспечение взаимодействия пользователя с системой через визуальный интерфейс, позволяя наблюдать и управлять блокировками.